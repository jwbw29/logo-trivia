import "@testing-library/jest-dom";
import { render, screen } from "@testing-library/react";
import Leaderboard from "@/app/leaderboard/page";

describe("Leaderboard Page", () => {
  //// Leaderboard heading
  it("Leaderboard heading renders", () => {
    render(<Leaderboard />);
    expect(false).toBe(true);
  });

  //// Home Icon
  it("Home Icon renders", () => {
    render(<Leaderboard />);
    expect(false).toBe(true);
  });

  //// Tabs from shadcn/ui
  it("Tabs from shadcn/ui renders", () => {
    render(<Leaderboard />);
    expect(false).toBe(true);
  });

  //// Mapped leaders and scores
  it("Mapped leaders and scores renders", () => {
    render(<Leaderboard />);
    expect(false).toBe(true);
  });
});
