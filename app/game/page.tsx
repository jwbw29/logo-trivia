import Image from "next/image";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { StartButton } from "@/components/Buttons";

export default function Game() {
  return <main className="flex flex-col min-h-screen p-2"></main>;
}
